import React from "react";
import Histogram from "../components/AHistogram";

const Starlink = () => {
    return (
        <div>
            <Histogram />
        </div>
    );
};

export default Starlink;
